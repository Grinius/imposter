# Set to the real production HTTPS origin before building for public launch.
# Omit for local development; no invented canonical domain is emitted.
SITE_URL=

# Public Stripe Payment Link for the premium unlock. Safe to commit the real
# value locally (it's not a secret) but it is NOT proof of payment on its own —
# see src/worker.ts's /api/premium/verify for the actual server-side check.
NEXT_PUBLIC_STRIPE_PAYMENT_LINK=
